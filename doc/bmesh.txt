
bmesh vs mesh


bmesh在编辑模式才有用
bpy.ops.mesh.select_all(action='SELECT') 选择所有
bpy.ops.mesh.select_random()
bpy.ops.mesh.select_mode(type='EDGE') 边选择模式
bmesh.ops

bm = bmesh.from_edit_mesh(obj.data)
bm.edges/faces/verts/loops
        loops?
        如何获得选择的面？
        选择面 bm.faces[0].select = True or select_set(True)
        bm.faces.active = None bmesh active的面
        bm.verts[0].normal
bm.verts[0].link_edges/faces
bm.verts/faces/edges.ensure_lookup_table()?